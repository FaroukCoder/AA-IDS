/**
 * monologue.js — Detective speech-bubble renderer (stub).
 * Full implementation deferred to a later phase.
 */

const Monologue = (() => {

  function clear()      { /* TODO: clear speech bubble */ }
  function add(text)    { /* TODO: append line to bubble */ }

  return { clear, add };

})();
