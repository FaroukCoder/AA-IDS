# Core pipeline
openai>=1.0.0
python-dotenv>=1.0.0
ipwhois>=1.3.0
requests>=2.32.0

# Terminal display
rich>=13.0.0

# Web server / dashboard
websockets>=13.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Packet capture — Docker victim container only
# Requires admin/root: on Windows run as Administrator; on Linux use sudo or setcap
scapy>=2.5.0

# SSH simulation — Docker attacker container only
paramiko>=3.0.0
