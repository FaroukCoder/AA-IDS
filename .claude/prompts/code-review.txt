Review the following code for security issues specific to this LLM-based
threat detection system.

Code to review:
{code}

Check for:
1. Hardcoded secrets (API keys, passwords, tokens)
2. User-controlled data injected into LLM prompts without _sanitize() call
3. LLM output used to invoke code or shell without REGISTRY validation
4. Missing IP format validation before any network call
5. Cache files (api_cache.json, llm_cache.json) contents logged to terminal
6. Tool failures that raise exceptions to the caller instead of returning
   a fallback AgentReport(fallback=True, confidence=0.0)

Respond with valid JSON only. No prose. No markdown fences.
{
  "issues": [
    {"severity": "CRITICAL|HIGH|MEDIUM|LOW", "location": "file:line", "description": "..."}
  ],
  "verdict": "APPROVE|WARN|BLOCK"
}
